exports.run = function(client, message, args, options) {

    // It's a distance calculator, it didn't make my math homework any easier
    
};
    
exports.conf = {
    name: "dist",
    aliases: [],
}