const unirest = require ("unirest");

exports.run = function(client, message, args, options) {

    // USES andruxnet's API TO GET A RANDOM MOVIE QUOTE
    
}
    
exports.conf = {
        name: "mquote",
        aliases: ["moviequote"],
}