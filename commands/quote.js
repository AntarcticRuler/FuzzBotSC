const unirest = require ("unirest");

exports.run = function(client, message, args, options) {

    // USES andruxnet's API TO GET A RANDOM QUOTE
    
}

exports.conf = {
        name: "quote",
        aliases: ["quotes"],
}