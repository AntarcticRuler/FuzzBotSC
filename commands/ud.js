const ud = require('urban-dictionary')

exports.run = function(client, message, args, options) {

    // SEARCHES UP A TERM ON URBAN DICTIONARY
    
}
    
exports.conf = {
    name: "ud",
    aliases: ["urban", "urbandictionary"],
}